// Attention to Detail — the thieving quest, read off
// `.planning/planning_quests/Attention to Detail.md`. A reporter pays for what
// can be learned about the duke, and the watching is done from above without
// being seen.
//
// A stub: the quest stands in the journal as something known of and never begun,
// and the `@@@` on its one stage is what is still owed. `npm run notes` lists it.

# info attention-to-detail
version: 0.1.0
dependencies:
  core
  tulsa

# quest watching-the-duke
title: Attention to Detail
log: Somebody in town pays for what can be learned about the duke, and the castle's upper windows face the roofs over the market.

stage watching:
  log: I took the work, and it is the sort that is over the moment anyone looks up. @@@ unwritten past this line: nothing begins this quest and nothing carries it. It wants a vantage on the duke from tulsa.market-rooftops - the watch there is written, and what it is worth seeing is this quest's to say.
  complete
