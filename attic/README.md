# Attic

These files are quarantined pre-chat-box-GUI salvage references: legacy-coupled code from the React GUI's old play + contribution layer that no longer compiles against the post-teardown content pipeline. They are intentionally **not** compiled (outside `src/`, excluded from `tsconfig`) and **not** tested (excluded from the vitest run). They will be mined for reusable presentational, theming, and test-harness patterns when the new chat-box GUI and agent-play CLI are built, then deleted.
