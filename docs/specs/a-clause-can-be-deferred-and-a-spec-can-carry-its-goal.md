# a-clause-can-be-deferred-and-a-spec-can-carry-its-goal

## Deliverable

A clause stands `met`, `unmet` or `unknown`. There is no way to say a branch deliberately dropped
one, so the honest moves are both bad: leave it outstanding, which blocks `merge-ready`'s clauses leg
forever because that leg is `outstandingClauses.length === 0`; or edit the spec, which rewrites the
contract silently and leaves the audit grading something nobody promised. Measured in a planning
session: adding c8 to `reimplement-localization` shifted every later clause and required rewiring
`--discharges` across three records by hand, with nothing checking the result — a fumble there would
have left a clause owed by nobody, and `merge-ready` would have passed a branch that dropped scope.

Add a fourth verdict, `deferred`, and make it cost something. It carries a reason, it does not count
as outstanding, and — the part that separates a deferral from a quiet scope cut — **it converts the
clause into a tracked `undelivered` record rather than deleting it.** That machinery already exists
and is already enforced: `Kind` includes `undelivered`, `Task.clause` is "the clause this record
*is*", and `doctor` errors both ways — an `undelivered` naming no clause, and a clause named by
anything else. A deferral has somewhere correct to go.

The second half comes from the same root. Clauses are written before the knowledge exists.
`audit-brief-arrives-complete` carried specific clauses while the actual goal was to settle the audit
prompt, which nobody could have foreseen when the clauses were written; task growth after a worker's
first round is recurring and usually *not* scope drift. Auditors already make evidence-based value
judgements, and today they make them against a clause list and nothing else.

**The two halves are one thing: the goal is what makes a deferral judgeable.** A clause may be
deferred when the spec's goal is still served, and an auditor with no recorded goal has nothing to
weigh that against — which is why "defer" without "goal" would just be a licence to drop scope.

| the situation | today | after |
| ------------- | ----- | ----- |
| a clause the branch could not reach | outstanding forever, or silently deleted | deferred with a reason, and tracked as `undelivered` |
| a clause the branch tried and failed | `unmet` | unchanged — `unmet` still means tried and failed |
| a clause nobody looked at | `unknown` | unchanged |
| an auditor judging whether a miss mattered | nothing recorded to judge against | the spec's goal, printed in its brief |
| dropped scope after the merge | gone | a record in the backlog naming the clause |

Proof:

- [c1] `deferred` is a fourth verdict, parsed, rendered and round-tripped like the other three, so a
  spec file carrying one reads back identically.
  proof: vitest scripts/lib/specDoc.test.ts
- [c2] A deferral must carry its reason. Recording `deferred` with no evidence is refused, because a
  deferral with no reason is an `unmet` clause with a shrug, and the one thing this must not become
  is a cheaper way to say unmet.
  proof: vitest scripts/tasks/audit.test.ts
- [c3] A deferred clause is not outstanding. `outstandingSummary` and `merge-ready`'s clauses leg both
  pass with one recorded, while the spec file goes on showing it as deferred with its reason — the
  gate stops blocking without the record being erased.
  proof: vitest scripts/lib/specDoc.test.ts
- [c4] A deferral does not lose the work. Recording one creates an `undelivered` record naming that
  clause, so scope the branch dropped becomes a tracked backlog item rather than a sentence in a spec
  nobody queries. `doctor` accepts the result, which is the existing invariant this rides on.
  proof: vitest scripts/tasks/audit.test.ts
- [c5] A spec carries a goal: one line, distinct from its clause list, stating what the branch is
  *for*. `audit-prompt` prints it, so the auditor has it without opening the file — the brief is
  generated and complete, and a goal it does not carry is a goal nobody reads.
  proof: vitest scripts/tasks/audit.test.ts
- [c6] The auditor is asked to judge against the goal. Its generated brief asks, in the step where
  verdicts are assigned, whether the branch served the goal where it missed a clause — the question
  that licenses a deferral, asked by the tool rather than remembered by the auditor.
  proof: vitest scripts/tasks/audit.test.ts
- [c7] A deferral is visible where decisions get reviewed, not swallowed by a green gate.
  `merge-ready` names the deferred clauses in the leg it now passes, so a branch that deferred its
  way to green says so in the same output that says it is ready.
  proof: vitest scripts/tasks/mergeReady.test.ts

## Goal

Give a spec an honest way to drop scope without either blocking merge forever or silently rewriting
its own contract.

## Decisions

- **`deferred` is not a synonym for `unmet`.** `unmet` means the branch tried and failed and the gate
  should block. `deferred` means the branch deliberately dropped it, the goal is still served, and
  the work is now tracked elsewhere. Collapsing them would keep the gate honest and lose the
  distinction that makes the honest move available; keeping only `unmet` is the status quo this
  branch exists to fix.
- **A deferral converts, it does not delete.** The alternative — dropping the clause from the spec —
  was available all along and is what people do today. It is refused because it rewrites a contract
  after the fact and leaves the audit grading a document that no longer says what was promised.
  Converting to `undelivered` keeps the promise visible and the work findable.
- **The reason is required, and required at the point of recording.** Not encouraged, not warned
  about. c2 is the whole defence against this becoming a shortcut, and a defence that can be skipped
  is not one.
- **The goal is one line.** `## Deliverable` prose already exists and is not it: prose is the argument,
  the goal is the thing the argument serves, and an auditor weighing a miss needs the second in a
  form it can hold. One line also makes it cheap enough to write that specs will actually carry one.
- **The auditor records; the author ratifies.** A deferral is a scope decision, and scope decisions
  are the author's. The auditor is well placed to *propose* one with evidence, and the recorded
  deferral is exactly the shape of thing `run-an-orchestrator-over-three-parallel-tasks` batches into
  one end-of-run review. This branch does not build a ratification step; it makes the proposal
  legible enough that reviewing a batch of them is a short job.
- **No new gate.** Nothing here refuses a merge that would have passed, and nothing polices whether a
  deferral was justified. The repository's stance is that a recorded fact is an agent's assertion an
  auditor can contradict, not something the tool adjudicates.
- **`mergeReady.ts` is shared with `a-branch-knows-which-spec-it-owes`, deliberately and by region.**
  c7 needs the `clauses` leg to name what it now passes over; that branch rewrites `authoredAsPlan`
  and `decideSpec`. Different functions in one file, which merge cleanly — measured on this
  repository, two edits to different regions of one file merged with no conflict. Neither branch
  touches the other's function, and this note exists in both specs so neither worker treats the
  other's presence as a surprise or a reason to widen its own grant.

## Open questions

- **Decided: a `## Goal` heading**, not a `Goal:` line inside `## Deliverable`. `parseSpecDoc` already
  finds every other section this way (`## Deliverable`, `## Decisions`, `## Audit passes`), so a
  fifth section reuses `sectionText` rather than adding a second parse shape; `parseGoal` reads the
  first non-blank line under it, so one line is what a reader — and the round-trip — gets.
- **Decided: left optional, and it does not read as orphaned.** A deferred clause's `undelivered`
  record gets `spec: null` rather than a new field naming a follow-up — the store already renders a
  null spec as `(deferred)`, and `tasks list --deferred` already means exactly "open task, no spec".
  Reusing that convention means the record surfaces in the general backlog and in `tasks list`'s
  default queue the moment it is created, the same way any other deferred task does; `source.spec`
  still names the spec it fell out of, which is what `spec show`'s owner lookup was widened
  (`clauseOwners`) to read so the clause does not report owned by nobody. No orphan: two existing
  queries already find it.
- **Decided: no.** The record ships with `writes: []` and `grant: null`, identical to the unmet path's
  undelivered record. `doctor`'s only grant warning fires on a non-null grant with an empty `writes`
  list, so leaving both null avoids it rather than needing a special case — there was nothing to
  inherit because the unmet path never set one either.

## Audit passes

### Pass 1 — 2026-08-06

- base: `a49a9b614c6cff533de973d9bceb9d18c4d42e94`
- head: `8fcc75fc58b33ef354652f1dbe5c899234247c90`
- proof 1: met — vitest scripts/lib/specDoc.test.ts "round-trips a deferred verdict, with its reason, the same way a met one round-trips its evidence" passes. Mutation-killed at named-test scope: dropping 'deferred' from VERDICTS (scripts/lib/specDoc.ts:16) fails exactly that test (1 failed of 40), because parseAuditPasses' proof regex is built from VERDICTS and stops recognising the line.
- proof 2: unmet — The truly-empty case is refused correctly (`--proof 1=deferred --evidence 1=` exits 1 with "clause 1 is deferred with no reason"), and the interactive walk correctly rejects a whitespace-only answer via `.trim() || null` (scripts/tasks/audit.ts:894). But the flag/file path — the one `--args-from` uses, the one the workflow calls "the one filing route for a branch audit" — never trims: `clauseScoped` (scripts/tasks/audit.ts:744-749) stores the raw slice after `=` verbatim, so `--evidence 1="   "` sets evidence to three spaces, which is truthy and survives the `!verdict.evidence` check at scripts/tasks/audit.ts:1069. Reproduced live in a scratch git fixture (store/systems/specs pointed at a temp dir via --store/--systems/--specs-dir/--branch): `audit demo-spec --proof 1=deferred --evidence "1=   " --proof 2=met --evidence "2=checked"` exits 0, writes `- proof 1: deferred — ` (trailing whitespace, no visible reason) into the spec file, and creates an undelivered task with `"evidence":"   "` in the store; `doctor` reports 0 errors against it. This is the exact "cheaper way to say unmet" c2's own text says must not be possible, and it is reachable through the primary, scriptable input route rather than an obscure corner.
- proof 3: met — vitest scripts/lib/specDoc.test.ts "takes a deferred clause off the outstanding list, the same way a met one is" passes; mutation-killed (removing `&& verdict.status !== 'deferred'` from outstandingSummary's filter fails that test, 1 failed of 40). Also verified against the real `runMergeReady` (scripts/tasks/mergeReady.ts), invoked directly via `npm run inspect` with a stubbed BranchStanding carrying `deferredClauses: ['c3']` and empty `outstandingClauses`: the clauses leg reports `ok: true`, `pass — the latest of 1 pass(es) leaves no clause outstanding; deferred: c3`, while nothing erases the spec file's own recorded verdict.
- proof 4: met — vitest scripts/tasks/audit.test.ts "c3/c4: is not outstanding, and converts into a tracked undelivered record with no spec — doctor accepts it" passes; mutation-killed (forcing `const deferred = false` at scripts/tasks/audit.ts:1094 fails that test, 1 failed of 102). Reproduced live: a deferred clause creates a `kind: undelivered, spec: null, clause: N, source: {spec, pass}` record; `doctor` reports 0 errors/warnings over it; it is findable via `tasks list --deferred` (matches `state: open && spec: null`, and no --kind filter excludes it) and via `tasks spec show <slug>` under "owed by" (clauseOwners was widened at scripts/tasks/specCmds.ts:174 to also match `kind === 'undelivered' && source.spec === spec`). Noted, not a finding: `tasks roadmap`'s backlog section (`scripts/lib/roadmap.ts:247`) filters `kind: 'task'` and folds all undelivered records — deferred or unmet — into an "other kinds" count rather than listing them individually. That is pre-existing behaviour for unmet-clause undelivered records too (unchanged by this branch), not a regression this branch introduced.
- proof 5: met — vitest scripts/tasks/audit.test.ts "is printed by audit-prompt without opening the file" passes; mutation-killed (retargeting parseGoal's sectionText call to a heading that does not exist fails that test, 1 failed of 40). `audit-prompt` on this very spec printed `Goal: Give a spec an honest way to drop scope without either blocking merge forever or silently rewriting its own contract.` before Step 1, confirming it in the generated brief this pass itself used.
- proof 6: met — vitest scripts/tasks/audit.test.ts "the step where verdicts are assigned asks whether the goal still holds before a clause is dropped" passes; mutation-killed (deleting the console.log line carrying "Ask this before recording unmet: does the goal still hold if this clause is never met?" at scripts/tasks/audit.ts:632 fails that test, 1 failed of 102). Confirmed present verbatim in this pass's own generated brief, step 3.
- proof 7: met — vitest scripts/tasks/mergeReady.test.ts "passes the clauses leg on a deferred clause, and names it in the same line that says pass" passes; mutation-killed (collapsing deferredNote to '' at scripts/tasks/mergeReady.ts:138 fails that test, 1 failed of 28). Verified against the real, unmodified `runMergeReady` via `npm run inspect` (not a re-implementation): stubbed standing with `deferredClauses: ['c3']`, `outstandingClauses: []` produces the single line `clauses ok pass — the latest of 1 pass(es) leaves no clause outstanding; deferred: c3` — c3 and c7 do not contradict; the same line that says the leg passes is the line that names what it passed over. Confirmed `mergeReady.ts` diff touches only `standingLegs`'s clauses leg and `branchStanding`'s returned fields (deferredClauses/outstandingClauses) — `authoredAsPlan` and `decideSpec`, the functions the sibling branch `a-branch-knows-which-spec-it-owes` owns, are called but not edited anywhere in this diff.

### Pass 2 — 2026-08-06

- base: `a49a9b614c6cff533de973d9bceb9d18c4d42e94`
- head: `07d1f491360ac903ae8b4e5de34e1dc24ac18e49`
- proof 1: met — Re-verified after 07d1f49: vitest scripts/lib/specDoc.test.ts "round-trips a deferred verdict, with its reason, the same way a met one round-trips its evidence" passes, unaffected by the clauseScoped trim fix. Mutation-killed at named-test scope this pass: dropping 'deferred' from VERDICTS (scripts/lib/specDoc.ts:16) fails exactly that test, 1 failed of 40, no escalation needed. No regression from 07d1f49, which touches only scripts/tasks/audit.ts and its test file.
- proof 2: unmet — Not closed. Pass 1's specific finding (plain ASCII whitespace via --evidence N="   ") is fixed: clauseScoped now trims at the assembly point, verified clean across the flag path, the --args-from path, and the interactive walk's own reason prompt, and JS's trim() also empties NBSP U+00A0, form feed, vertical tab and BOM U+FEFF for free since those are in ECMA-262's WhiteSpace/LineTerminator sets. But trim() only strips Unicode "White_Space" codepoints, not the broader family of zero-width and invisible format characters, so a value made entirely of one of those survives as a truthy, non-empty string that renders identically to nothing in a terminal, an editor, or the merged spec file. Reproduced live through all three input routes with the evidence value set to a single U+200B (zero-width space): the direct --evidence flag, an --args-from file carrying the character in the N=value slice, and the interactive walk's "reason (required for deferred)" prompt all accept it, exit 0, write "- proof 1: deferred — " into the spec file with the invisible character trailing the dash, and create an undelivered task whose stored evidence is the single ZWSP character; doctor reports 0 errors against the result. The same gap reproduces with ZWNJ U+200C, ZWJ U+200D, soft hyphen U+00AD, word joiner U+2060, left-to-right mark U+200E and the deprecated Mongolian vowel separator U+180E, so this is a property of trim()'s character class, not a one-off. This is exactly the "cheaper way to say unmet" c2's own text forbids, reached through the same three routes pass 1 exercised, with input a human reviewing the terminal or the spec diff cannot distinguish from empty. Note for scope: the identical gap exists for met and predates this branch, unchanged since a49a9b6 — consistent with the branch's own framing that the met-side hole was pre-existing and sanctioned, not something 07d1f49 introduced or made worse. Also worth recording: the test 07d1f49 added to prove the fix, "c2: whitespace-only evidence is refused the same way no evidence is, for met as well as deferred", exercises only ASCII space and tab, exactly the character class trim() is guaranteed to handle, so it mutation-kills cleanly but never exercises the character class that still gets through — a test tied to the mechanism (trim strips JS whitespace) rather than the property the clause promises (a reason a human can read). A punctuation-only value such as "---" was also tried and is accepted; that is a weaker case since it is at least visible content, and it is not the basis for this unmet verdict, but it shows the guard has no concept of "reason" beyond non-empty-after-trim.
- proof 3: met — Re-verified after 07d1f49: vitest scripts/lib/specDoc.test.ts "takes a deferred clause off the outstanding list, the same way a met one is" passes. Mutation-killed at named-test scope this pass: removing "&& verdict.status !== 'deferred'" from outstandingSummary's filter fails exactly that test, 1 failed of 40, no escalation. Unaffected by the clauseScoped trim change, which is confined to audit.ts's flag parsing and does not touch specDoc.ts's outstandingSummary.
- proof 4: met — Re-verified after 07d1f49: vitest scripts/tasks/audit.test.ts "c3/c4: is not outstanding, and converts into a tracked undelivered record with no spec — doctor accepts it" passes. Mutation-killed at named-test scope this pass: forcing "const deferred = false" at scripts/tasks/audit.ts:1094 fails exactly that test, 1 failed of 105, no escalation. Also independently re-verified the multi-line-reason concern this pass was asked to check: an --args-from file with a two-line continuation reason for a deferred clause round-trips both lines whole into the spec file ("- proof 1: deferred — the goal is served without it:\n  see the diff for the reasoning") and into "tasks show" for the undelivered record, which prints both lines under its evidence field. The trim fix removes only the block's own outer whitespace, confirmed not to touch interior newlines or words in either destination.
- proof 5: met — Re-verified after 07d1f49: vitest scripts/tasks/audit.test.ts "is printed by audit-prompt without opening the file" passes. Mutation-killed at named-test scope this pass: retargeting parseGoal's sectionText call to a heading that does not exist fails exactly that test, 1 failed of 105, no escalation. audit-prompt on this very spec again printed the Goal line before Step 1 this pass, confirming it in the generated brief this pass itself used. Unaffected by 07d1f49.
- proof 6: met — Re-verified after 07d1f49: vitest scripts/tasks/audit.test.ts "the step where verdicts are assigned asks whether the goal still holds before a clause is dropped" passes. Mutation-killed at named-test scope this pass: deleting the console.log line carrying "Ask this before recording unmet" at scripts/tasks/audit.ts:632 fails exactly that test, 1 failed of 105, no escalation. Confirmed present verbatim in this pass's own generated brief, step 3. Unaffected by 07d1f49.
- proof 7: met — Re-verified after 07d1f49: vitest scripts/tasks/mergeReady.test.ts "passes the clauses leg on a deferred clause, and names it in the same line that says pass" passes. Mutation-killed at named-test scope this pass: collapsing deferredNote to '' at scripts/tasks/mergeReady.ts:138 fails exactly that test, 1 failed of 28, no escalation. Re-ran npm run tasks -- merge-ready on this branch directly: clauses leg reports FAIL because c2 is genuinely outstanding after this pass, which is the correct behavior, not a c7 defect — c7 is about naming deferred clauses in the passing line, and mergeReady.ts is untouched by 07d1f49. Unaffected by the trim fix.

### Pass 3 — 2026-08-06

- base: `a49a9b614c6cff533de973d9bceb9d18c4d42e94`
- head: `01e04a192dd771d79523e08a4ff328d482485eac`
- proof 1: met — Re-verified independently on 01e04a1: vitest scripts/lib/specDoc.test.ts "round-trips a deferred verdict, with its reason, the same way a met one round-trips its evidence" passes (40/40 in the file). Mutation-killed this pass at file scope with a hand-built manifest: dropping 'deferred' from VERDICTS at scripts/lib/specDoc.ts:16 fails exactly 1 of 40 tests in that file, no escalation to the whole suite needed. specDoc.ts is untouched by 01e04a1, which edits only scripts/tasks/audit.ts and its test file, so nothing about this pass's changes could have regressed c1.
- proof 2: unmet — Not closed a third time. Pass 1 closed plain ASCII whitespace; pass 2 closed the Unicode zero-width/format family (category Cf) by replacing the bare-truthiness check with hasVisibleContent, a regex of the shape [^\s\p{Cf}]/u applied identically at the --evidence flag site (audit.ts:1092), the --args-from file path (same site, since --args-from just feeds the same argv parser), and the interactive walk's evidence loop (audit.ts:907-921, now correctly re-prompting rather than accepting the first non-empty answer -- verified live that exhausting the prompter on repeated invisible-only answers aborts the whole walk with no verdict recorded and no spec-file write, rather than silently accepting the last answer). That fix holds for everything in category Cf. The gap this pass found is a sibling category the same regex does not cover: Unicode category Cc, the C0/C1 control characters -- NUL, BEL, ESC, DEL and the rest of the non-printing control range. None of these are Unicode White_Space and none are category Cf (Format); they are category Cc (Control), which VISIBLE_CHARACTER's negated class [^\s\p{Cf}] does not exclude, so hasVisibleContent(x07) and hasVisibleContent(x00) and hasVisibleContent(x1b) and hasVisibleContent(x7f) all return true. Reproduced live through all three routes with a git-fixture-backed CLI run built on cliFixtures.ts's fixture()/audit()/auditWith() helpers, using a single BEL (0x07) as the entire reason: the direct --evidence flag, an --args-from file carrying the same byte in the N=value slice, and the interactive walk's "reason (required for deferred)" prompt all accept it, exit 0, and write "- proof 1: deferred -- " into the spec markdown file with a literal, un-escaped BEL control byte trailing the dash; tasks list --kind undelivered shows the record created with that same byte as its stored evidence. This is not merely unreadable the way the Cf gap was -- BEL triggers an audible/visual bell in a terminal that later cats or greps the spec file, ESC can open a live ANSI escape sequence (confirmed: --evidence "1=\x1b[31mFAKE ERROR\x1b[0m" is accepted and the raw escape bytes land in the spec file, so a later terminal render of that pass's history paints attacker-chosen styled text rather than displaying the bytes literally), and none of this is caught downstream either: merge-ready's own "bytes" leg (scripts/lib/bytes.ts:25) only refuses a NUL byte specifically ("grep treats this file as binary"), so a NUL-only reason would eventually be caught there if committed, but BEL/ESC/DEL and every other non-NUL C0/C1 control sail through both c2's guard and the repo's own byte-integrity gate with nothing left to catch them. This is exactly the "cheaper way to say unmet" c2's own text forbids, reached through the same three routes the last two passes exercised. Evaluated the rest of the candidate list the brief for this pass named, to draw the line rather than extend it indefinitely: a single punctuation mark (hasVisibleContent("-") is true) and a long run of one character (50 "!" is true) are genuinely visible content -- accepting them is correct, not a defect, and demanding "meaningful" content would be scope creep past what c2 promises ("a reason a human can read", not "a good reason"). A lone combining mark (U+0301 alone) also returns true and, unlike a control character, does render a visible glyph (a floating diacritic over a dotted circle in most terminals/editors) -- ugly but readable, so not flagged as a defect either. An unpaired UTF-16 surrogate (U+D800 alone) returns true from hasVisibleContent on the raw string, but Node's UTF-8 file write silently replaces it with U+FFFD (the visible replacement-character glyph) before it ever reaches disk, confirmed by writing it through the same fixture and reading the spec file back -- so it self-resolves into visible content on the one path that matters (what lands in the committed file) and is not a live bypass. RTL/LTR override characters (e.g. U+202E alone) are already category Cf and are correctly rejected today, matching pass 2's fix. The recommended fix, stated as a line rather than a list: extend VISIBLE_CHARACTER to also exclude \p{Cc}, i.e. /[^\s\p{Cf}\p{Cc}]/u -- no legitimate audit reason is written using literal control bytes, so excluding the whole Control category closes this without rejecting anything a human would actually type.
- proof 3: met — Re-verified independently: vitest scripts/lib/specDoc.test.ts "takes a deferred clause off the outstanding list, the same way a met one is" passes. Mutation-killed this pass at file scope: changing outstandingSummary's filter at scripts/lib/specDoc.ts:281 from "verdict.status !== 'met' && verdict.status !== 'deferred'" to "verdict.status !== 'met'" fails exactly 2 of 40 tests in the file, no escalation. Confirmed live against the real merge-ready gate on this branch (which currently reports the clauses leg as FAIL because c2 is genuinely outstanding, the correct behavior, not a c3 defect) that a deferred verdict does not itself appear in outstandingSummary's text. Unaffected by 01e04a1, which does not touch specDoc.ts.
- proof 4: met — Re-verified independently: vitest scripts/tasks/audit.test.ts "c3/c4: is not outstanding, and converts into a tracked undelivered record with no spec -- doctor accepts it" passes. Mutation-killed this pass: forcing "const deferred = false" at scripts/tasks/audit.ts:1117 fails exactly 3 of 108 tests in the file, no escalation. Independently re-verified the multi-line-reason round trip this pass was specifically asked to re-check, since the guard sites moved this round: an --args-from file with a two-line continuation reason for a deferred clause still round-trips both lines whole into the spec file and into "tasks show" for the undelivered record -- 01e04a1 does not touch clauseScoped's own trim() call, only the separate hasVisibleContent check applied to its already-assembled output, so the multi-line assembly path this proof depends on is untouched by this pass's diff. Also confirmed doctor reports 0 errors over a deferred clause's undelivered record on this branch's own current tree.
- proof 5: met — Re-verified independently: vitest scripts/tasks/audit.test.ts "is printed by audit-prompt without opening the file" passes. Mutation-killed this pass: retargeting parseGoal's sectionText call at scripts/lib/specDoc.ts:216 to a heading that does not exist fails exactly 1 of 108 tests in scripts/tasks/audit.test.ts, no escalation. This pass's own audit-prompt run on this very spec printed "Goal: Give a spec an honest way to drop scope without either blocking merge forever or silently rewriting its own contract." before Step 1, confirming it in the brief this pass itself used. Unaffected by 01e04a1.
- proof 6: met — Re-verified independently: vitest scripts/tasks/audit.test.ts "the step where verdicts are assigned asks whether the goal still holds before a clause is dropped" passes. Mutation-killed this pass: deleting the console.log line carrying "Ask this before recording unmet" at scripts/tasks/audit.ts:632 fails exactly 2 of 108 tests in the file, no escalation. Confirmed present verbatim in this pass's own generated brief, step 3. Unaffected by 01e04a1.
- proof 7: met — Re-verified independently: vitest scripts/tasks/mergeReady.test.ts "passes the clauses leg on a deferred clause, and names it in the same line that says pass" passes. Mutation-killed this pass: collapsing deferredNote to '' at scripts/tasks/mergeReady.ts:138 fails exactly 2 of 28 tests in the file, no escalation. Re-ran merge-ready directly on this branch: the clauses leg correctly reports FAIL naming c2 as outstanding after this pass, which is c2's own genuine failure, not a c7 defect -- mergeReady.ts is untouched by 01e04a1.

### Pass 4 — 2026-08-06

- base: `a49a9b614c6cff533de973d9bceb9d18c4d42e94`
- head: `da469acdeba82ec2f35384f1241b425b435784ee`
- proof 1: met — Re-verified independently on da469ac (pass 4). vitest scripts/lib/specDoc.test.ts "round-trips a deferred verdict, with its reason, the same way a met one round-trips its evidence" passes, 40/40 in the file. Mutation-killed this pass with a hand-built manifest: dropping 'deferred' from VERDICTS at scripts/lib/specDoc.ts:16 fails exactly that test, 1 failed of 40, no escalation needed. da469ac touches only docs/events.jsonl, scripts/tasks/audit.ts and scripts/tasks/audit.test.ts, so specDoc.ts and this clause are untouched by pass 4's diff and nothing here could have regressed.
- proof 2: unmet — Not closed a fourth time, though pass 4's own fix is correctly implemented. da469ac's diff is exactly what its commit message states: VISIBLE_CHARACTER changed from /[^\s\p{Cf}]/u to /[^\s\p{Cf}\p{Cc}]/u, and nothing more. That regex is a faithful implementation of the adopted sentence's three named categories: JS's \s matches ECMA-262 WhiteSpace/LineTerminator, which is exactly Unicode's Zs/Zl/Zp plus tab/lf/cr/vt/ff -- "whitespace" in the ordinary sense; \p{Cf} matches General_Category=Format exactly; \p{Cc} matches General_Category=Control exactly. Re-verified live through all three routes with a single BEL, a single ESC, a single NUL, a single DEL and both C1 range endpoints (U+0080, U+009F): all six are now refused with "clause 1 is deferred with no reason" via the direct --evidence flag, via an --args-from file (--proof/--evidence lines with the slug kept on the command line, since --args-from rejects a slug on the file's own first line), and via the interactive walk's "reason (required for deferred)" prompt, which now correctly loops rather than accepting the byte. Mutation-killed at named-test scope: reverting the regex to pass 3's /[^\s\p{Cf}]/u fails exactly the new "c2: a reason made only of control characters..." test, 1 of 110 in the file, no escalation. So question one -- is the rule correctly implemented -- is yes.
  Question two -- is the rule closed under its own logic -- is no. The sentence's justification treats "whitespace / Cf / Cc" as if it exhausted "does not occupy space when rendered," but Unicode's own canonical property for exactly that concept, Default_Ignorable_Code_Point (supported directly by Node's regex engine, confirmed by testing /\p{Default_Ignorable_Code_Point}/u), is not equal to that union. Scanning U+0000-U+2FFFF for codepoints that are Default_Ignorable_Code_Point=true but neither whitespace, Cf, nor Cc turns up 37 characters, none of them exotic: COMBINING GRAPHEME JOINER U+034F (category Mn, standardised as glyphless by design), HANGUL FILLER U+115F/U+1160/U+3164/HALFWIDTH HANGUL FILLER U+FFA0 (category Lo, deliberately blank placeholders in Hangul composition), KHMER VOWEL INHERENT AQ/AA U+17B4/U+17B5 (Mn, invisible inherent-vowel signs), Mongolian free variation selectors U+180B-U+180D/U+180F (Mn), and -- the practically common case -- all sixteen BMP variation selectors U+FE00-U+FE0F (category Mn, not Cf; VS16 alone is the emoji-presentation selector seen constantly in real text, just never legitimately alone). Every one of these is documented by Unicode as intentionally non-rendering when not attached to a base character, yet every one returns true from hasVisibleContent, because Mn and Lo are not excluded by the current regex. Reproduced live on all three routes with a reason of two lone VS16 characters (U+FE0F U+FE0F) as the entire evidence: accepted, exit 0, through the --evidence flag, through --args-from, and through the interactive walk, writing a deferred verdict whose stored reason a human reading the spec file or the terminal sees as nothing. This is exactly the "cheaper way to say unmet" c2 forbids, and it is not "another codepoint" in the sense pass 3 already closed off -- it is a demonstration that the sentence's three-category model is an incomplete proxy for its own stated principle ("occupies space when rendered"), because Unicode itself defines invisible characters outside category Cf for technical reasons (canonical-combining-class control, Hangul-jamo filling, emoji presentation selection) that have nothing to do with "format" in the Cf sense.
  Guarding the other direction, as asked: ordinary prose in English, CJK, Arabic, Hebrew and Devanagari; a lone punctuation mark; an 80-character run of one character; a lone combining mark (U+0301, which does render a visible dotted-circle diacritic and is not default-ignorable); an unpaired surrogate; and a multi-line reason through --args-from (verified this pass: a two-line evidence value for a deferred clause round-trips both lines whole into the spec file) are all still correctly accepted -- confirmed live, not merely re-run from memory. The emoji-ZWJ case asked for specifically: a four-person family emoji (base emoji joined by three ZWJ, U+200D, each Cf) is accepted, and a rainbow-flag sequence using VS16 (U+FE0F, one of the newly-found gap characters) plus ZWJ is also accepted -- but confirmed this is on the strength of the emoji glyphs themselves (category So, never excluded), not the joiners or selectors: a string of nothing but three ZWJ characters (no glyphs at all) is correctly rejected, proving the guard does not mistake "contains a joiner" for "contains content." A ZWJ sequence built from real emoji will keep passing under the recommended fix below, since the base glyphs are not Default_Ignorable; only a reason with no base glyphs at all -- lone selectors or fillers -- would newly be refused.
  Recommended fix, stated as a line rather than a list, matching the branch's own "one line" style: replace \p{Cf} with \p{Default_Ignorable_Code_Point} in VISIBLE_CHARACTER, i.e. /[^\s\p{Cc}\p{Default_Ignorable_Code_Point}]/u. This is a correction to what the regex uses to mean "renders nothing," not a new exclusion layered on top of "no further exclusions" -- verified this candidate against the full existing accepted/rejected matrix (every invisible case pass 1-3 and this pass require rejecting, and every visible case, including the CJK/Arabic/Hebrew/Devanagari/emoji-ZWJ/combining-mark/surrogate cases above) and it produces the correct verdict on every one, with no regression.
- proof 3: met — Re-verified independently: vitest scripts/lib/specDoc.test.ts "takes a deferred clause off the outstanding list, the same way a met one is" passes. Mutation-killed this pass: changing outstandingSummary's filter at scripts/lib/specDoc.ts:281 from "verdict.status !== 'met' && verdict.status !== 'deferred'" to "verdict.status !== 'met'" fails exactly that test, 1 of 40, no escalation. specDoc.ts is untouched by da469ac, so nothing about pass 4's diff could regress this clause; re-ran merge-ready directly on this branch and confirmed a deferred verdict does not itself appear in outstandingSummary's text.
- proof 4: met — Re-verified independently: vitest scripts/tasks/audit.test.ts "c3/c4: is not outstanding, and converts into a tracked undelivered record with no spec -- doctor accepts it" passes. Mutation-killed this pass: forcing "const deferred = false" at scripts/tasks/audit.ts:1120 (the guard site moved one line since pass 3's 1117, still inside cmdAudit) fails exactly that test, 1 of 110, no escalation. Independently re-verified the multi-line-reason round trip this pass was specifically asked to re-check: a two-line reason for a deferred clause, supplied through --args-from with the continuation line unprefixed, lands whole into the spec file's "- proof 1: deferred --" line with both lines intact and into the undelivered task's stored evidence field, confirmed by reading both files back after the run. da469ac does not touch clauseScoped's assembly or the deferred-conversion logic, only the separate visibility check applied to its output, so this proof is unaffected. Also re-ran merge-ready directly: doctor reports 0 errors/warnings against this branch's own deferred-clause undelivered record.
- proof 5: met — Re-verified independently: vitest scripts/tasks/audit.test.ts "c5: is printed by audit-prompt without opening the file" passes. Mutation-killed this pass: retargeting parseGoal's sectionText call at scripts/lib/specDoc.ts:216 to a heading that does not exist fails exactly that test, 1 of 110, no escalation. This pass's own audit-prompt run on this very spec printed "Goal: Give a spec an honest way to drop scope without either blocking merge forever or silently rewriting" before Step 1, confirming it in the brief this pass itself used. Unaffected by da469ac, which does not touch specDoc.ts.
- proof 6: met — Re-verified independently: vitest scripts/tasks/audit.test.ts "c6: the step where verdicts are assigned asks whether the goal still holds before a clause is dropped" passes. Mutation-killed this pass: deleting the console.log line carrying "Ask this before recording unmet" at scripts/tasks/audit.ts:632 fails exactly that test, 1 of 110, no escalation. Confirmed present verbatim in this pass's own generated brief, step 3, reproduced word for word in the "deferred" verdict description this pass received. Unaffected by da469ac.
- proof 7: met — Re-verified independently: vitest scripts/tasks/mergeReady.test.ts "passes the clauses leg on a deferred clause, and names it in the same line that says pass" passes. Mutation-killed this pass: collapsing deferredNote to '' at scripts/tasks/mergeReady.ts:138 fails exactly that test, 1 of 28, no escalation. Re-ran merge-ready directly on this branch: the clauses leg correctly reports FAIL naming c2 as outstanding after this pass -- c2's own genuine, re-confirmed failure, not a c7 defect -- and mergeReady.ts is untouched by da469ac.

### Pass 5 — 2026-08-06

- base: `d1ea4345f8e4dace868c8518b670e4d39aa8f780`
- head: `0dbf2f4cbe2f45ba14bc98ec02b84e373a09c1c0`
- proof 1: met — Independently re-verified on 0dbf2f4: vitest scripts/lib/specDoc.test.ts "round-trips a deferred verdict, with its reason, the same way a met one round-trips its evidence" passes, 40/40 in the file. Mutation-killed this pass at named-test scope with a hand-built manifest (no manifest could be generated by audit-prompt itself this pass -- see the finding filed below): dropping 'deferred' from VERDICTS at scripts/lib/specDoc.ts:16 fails exactly that test, 1 failed of 40, no escalation needed. specDoc.ts is untouched by the only commit unique to this pass (8c3c015), so nothing here could have regressed since pass 4.
- proof 2: met — Closed. Pass 4's fix -- swapping the hand-picked \p{Cf} proxy for \p{Default_Ignorable_Code_Point}, the Unicode property that defines "occupies no space when rendered" -- is the right fix, and this pass confirms it rather than finding a fifth class. The argument: VISIBLE_CHARACTER is now /[^\s\p{Default_Ignorable_Code_Point}\p{Cc}]/u. \s covers ordinary whitespace exactly (ECMA-262 WhiteSpace/LineTerminator, a superset of Unicode White_Space that also folds in U+FEFF, itself already Cf/DI so this is not a gap either way). \p{Cc} covers every C0/C1 control byte exactly -- there is no "commands the renderer" character outside Cc that DI does not already also catch, because the DI formula includes Cf (which is where bidi override/embedding characters such as RLO U+202E live) and excludes only a specific, named carve-out: Prepended_Concatenation_Mark characters (Arabic number sign U+0600-0605, Syriac abbreviation mark U+070F, Arabic disputed end of ayah U+08E2, Kaithi number signs U+110BD/U+110CD, etc.), which Unicode itself keeps out of Default_Ignorable_Code_Point specifically because rendering one in isolation is not guaranteed invisible -- i.e. Unicode's own judgment agrees with the sentence that these occupy space, so treating them as visible is correct, not a gap. I verified this directly rather than taking pass 4's word for it: ran hasVisibleContent (scripts/tasks/audit.ts) against 28 boundary cases via npm run inspect, covering every character pass 1-4 individually found plus the Prepended_Concatenation_Mark exceptions and a bare tag-character sequence with no base glyph. Every invisible/control case (ASCII whitespace, BOM, ZWSP, RLO, NUL/BEL/DEL/C1, CGJ, VS16 alone, bare tag sequence with no base emoji) returned false; every genuinely visible case (Arabic number sign alone, Syriac abbreviation mark alone, Kaithi number sign alone, a lone combining acute, a combining enclosing keycap, single punctuation, a long run of one character, an unpaired surrogate) returned true, matching the sentence in every case including the ones outside what any of the four passes' own test suites individually cover. Then re-verified the same matrix end to end through the real CLI's --args-from route (not just the unit function) using scripts/tasks/cliFixtures.ts's fixture()/audit() helpers against a real demo-spec fixture: ordinary English prose, Hebrew prose, Chinese prose, a punctuation-only reason, a four-person-family ZWJ emoji sequence, and a genuine two-line multi-line reason all recorded a deferred verdict with status 0 and no refusal; a reason made only of ZWSP characters and a reason made only of a single BEL both correctly hit "clause 1 is deferred with no reason" with status 1; and the Arabic-number-sign-alone case correctly passed as visible, confirming the DI exclusion is intentional and matches the sentence rather than being an oversight. Read the multi-line case's actual on-disk effect too: the two-line reason landed whole into the spec file's "- proof 1: deferred --" line and into the undelivered task's stored evidence, and doctor reported 0 errors/warnings over the result. Mutation-killed at named-test scope: reverting the regex to pass 3's /[^\s\p{Cf}\p{Cc}]/u fails exactly "c2: a reason made only of default-ignorable characters outside category Cf is refused (VS16, CGJ, Hangul filler jamo, Khmer inherent vowel sign, Mongolian free variation selector)", 1 of 112 in the file, no escalation. Both questions this pass was asked to answer -- does the union cover everything the sentence excludes, and does it exclude nothing the sentence permits -- are yes, on evidence independent of the prior four passes' own test suites, not merely a re-run of them.
- proof 3: met — Independently re-verified on 0dbf2f4: vitest scripts/lib/specDoc.test.ts "takes a deferred clause off the outstanding list, the same way a met one is" passes. Mutation-killed this pass at named-test scope: changing outstandingSummary's filter at scripts/lib/specDoc.ts:281 from "verdict.status !== 'met' && verdict.status !== 'deferred'" to "verdict.status !== 'met'" fails exactly that test, 1 of 40, no escalation. specDoc.ts is untouched by 8c3c015 (this pass's only unique commit), so nothing here could have regressed since pass 4. Re-ran merge-ready directly on this branch and confirmed a deferred verdict does not itself appear in outstandingSummary's text.
- proof 4: met — Independently re-verified on 0dbf2f4: vitest scripts/tasks/audit.test.ts "c3/c4: is not outstanding, and converts into a tracked undelivered record with no spec -- doctor accepts it" passes. Mutation-killed this pass at named-test scope: forcing "const deferred = false" at scripts/tasks/audit.ts:1124 (the guard site moved since pass 4's 1120, still inside cmdAudit) fails exactly that test, 1 of 112, no escalation. Re-confirmed the multi-line-reason round trip live through the real CLI this pass, not merely re-read from a prior pass's claim: a two-line deferred reason recorded through --args-from lands whole into the spec file's "- proof N: deferred --" line and into the undelivered task's stored evidence, and doctor reports 0 errors/warnings over the result on a fresh fixture. Also re-ran merge-ready directly on this branch's own real tree: doctor reports 0 errors/warnings against this branch's own deferred-clause undelivered record, and the store's tasks.jsonl (620 records, checked for id collisions after this branch's merge from main) has no duplicate ids.
- proof 5: met — Independently re-verified on 0dbf2f4: vitest scripts/tasks/audit.test.ts "c5: is printed by audit-prompt without opening the file" passes. Mutation-killed this pass at named-test scope: retargeting parseGoal's sectionText call at scripts/lib/specDoc.ts:216 to a heading that does not exist fails exactly that test, 1 of 112, no escalation. This pass's own audit-prompt run on this very spec printed "Goal: Give a spec an honest way to drop scope without either blocking merge forever or silently rewriting" before Step 1, confirming it in the brief this pass itself used. specDoc.ts is untouched by 8c3c015.
- proof 6: met — Independently re-verified on 0dbf2f4: vitest scripts/tasks/audit.test.ts "c6: the step where verdicts are assigned asks whether the goal still holds before a clause is dropped" passes. Mutation-killed this pass at named-test scope: deleting the console.log line carrying "Ask this before recording unmet" at scripts/tasks/audit.ts:632 fails exactly that test, 1 of 112, no escalation. Confirmed present verbatim in this pass's own generated brief, step 3. Unaffected by 8c3c015, whose diff to audit.ts is confined to VISIBLE_CHARACTER, clauseScoped's doc comment, and the deferred-verdict description text -- confirmed by reading the commit's diff directly rather than trusting its message.
- proof 7: met — Independently re-verified on 0dbf2f4: vitest scripts/tasks/mergeReady.test.ts "passes the clauses leg on a deferred clause, and names it in the same line that says pass" passes. Mutation-killed this pass at named-test scope: collapsing deferredNote to '' at scripts/tasks/mergeReady.ts:138 fails exactly that test, 1 of 59, no escalation. Re-ran merge-ready directly on this branch: the clauses leg correctly reports FAIL naming c2 as outstanding -- because the spec file's own recorded audit passes still show pass 4's unmet c2 verdict until this pass files -- which is expected and not a c7 defect; mergeReady.ts is untouched by 8c3c015. Also confirmed this pass that merge-ready's own spec-source leg resolves the correct spec via decideSpec (a-branch-knows-which-spec-it-owes's gate), naming this spec correctly and explicitly rejecting run-an-orchestrator-over-three-parallel-tasks as "not shown to be touched by this branch's diff" -- see the finding below on why audit-prompt's own warning disagrees with that same gate.
