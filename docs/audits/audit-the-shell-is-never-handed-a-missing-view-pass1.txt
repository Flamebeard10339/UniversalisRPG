# Pass 1 on the-shell-is-never-handed-a-missing-view. Fill in every value, then run:
#   npm run tasks -- audit the-shell-is-never-handed-a-missing-view --args-from <this file>
# One flag per line. A line that does not open with -- continues the value above it,
# which is how a clause's evidence can be a paragraph. `#` at column zero is a comment.

# met | unmet | unknown | deferred. `met` needs evidence the next pass can re-run and is
# refused without one; `unmet` means you checked and it fails; `unknown` means nobody looked;
# `deferred` means you checked, it fails, and the goal this brief printed still holds without
# it — refused with no reason, and converts the clause into a tracked undelivered record.

# [c1] **The published type says what is true.** `DriverSnapshot.view` is `PlayView`. The harness publishe…
--proof 1=unmet
--evidence 1=The three subjects the clause names are all true: src/ui/driver.ts:30 is `view: PlayView`,
src/ui/agent/testHarness.ts:38 is `view: PlayView`, src/ui/App.tsx:150 `standingIn` is total, and
`npx tsc --noEmit` passes (re-run: it did on 7051d8b). The clause fails on its own subject, which is
the published type and not that one field of it. Two more fields of the same interface, in the same
file, still describe states the door can no longer produce, and both were made unreachable by this
branch's own predecessor commit cedfaf5:
(a) src/ui/driver.ts:43 `speed: number | null`, while src/ui/driver.ts:151 sets it from
`context.live.speed` and src/runtime/command.ts:131 types that `speed: number`. `context` is
definitely assigned by `openOnce` before the constructor returns, so null is unreachable. The lie has
already generated the defect the Deliverable warns about: src/ui/SettingsPane.tsx:62 reads
`{speed === null ? null : <SpeedField ... />}`, an unreachable branch in a rendered component. Measured
with `npm run mutate` on 2026-08-17 over the whole suite: removing that guard SURVIVED 0 failed of
3749, and inverting it — which takes the speed control off the Settings page entirely — also SURVIVED
0 failed of 3749. Manifest kept at
C:\Users\yonat\AppData\Local\Temp\audit-the-shell-is-never-handed-a-missing-view-pass1-residue.json.
So it is dead and untested at once, which the Deliverable names as the confusion that cost the
predecessor branch its HIGH.
(b) src/ui/driver.ts:66 `serialized(): string | null` with the comment "null when there is no session
to save", while the body is `serializeSession(context.session)` and src/runtime/session.ts:395 returns
`string`. At the merge base 7a0081a the body was `context ? serializeSession(context.session) : null`,
so the null was real then and is not now. It already carries the spelling this spec exists to refuse:
scripts/drift.test.ts:236 `JSON.parse(gui.serialized()!)`.
The sweep was aimed at one field of the published type rather than at the type, and `npx tsc --noEmit`
is a proof that cannot see the difference — it passed before the sweep and passes now.

# [c2] **Nothing under `src/ui` asks whether there is a view.** The count of the spellings that test for o…
--proof 2=unmet
--evidence 2=The scanner works for what it enumerates, and it enumerates. Eight aimed mutations, all
KILLED by the test that names them (`npm run mutate`, 2026-08-17, manifest at
C:\Users\yonat\AppData\Local\Temp\mutations-the-shell-is-never-handed-a-missing-view-pass1.json):
reintroducing `view?.` in src/ui/LocationBanner.tsx, reintroducing `view!` in src/ui/App.tsx,
reintroducing `view?.` in the test file src/ui/mapEdit.test.ts, restoring `PlayView | null` on
`DriverSnapshot`, stopping the walk descending into tests, dropping one row from `ASKING`, dropping
`undefined` from `NOTHING`, and dropping the chain prefix from `A_VIEW`. `vitest src/ui/surface.test.ts`
passes 32 of 32 and the tree is clean of all eight spellings.
What fails is the clause's operative claim — "derived ... so a site written next month is caught rather
than a list going stale". The subjects (the files) are derived; the predicate (what counts as asking) is
a hand-written list of eight spellings, and the test that grades it ('fires on every way of asking, and
on none of the ways of using') checks that list against a second hand-written list of the same eight,
which is enumeration on both axes. Seven ordinary spellings pass the whole rule. Re-runnable — paste the
file's own `A_VIEW`, `NOTHING`, `ASKING` and `UNION` into `node -e` and test these strings:
  `view?: PlayView`            escapes the union rule entirely — the optional-property spelling of the
                               very union it bans, and the spelling `CommandResult.view?: PlayView` in
                               src/runtime/command.ts:102 already uses one layer down
  `if (view) ...`              escapes all eight — the plainest way of asking there is
  `view == null` / `!= null`   escapes: the pattern requires `[=!]==`, three characters
  `typeof view === 'undefined'` escapes: the quote breaks the `NOTHING` alternation
  `Boolean(view)`              escapes
  `const at = view\n  ? 1 : 2` escapes: the scan is line-by-line, so a formatter wrapping a long
                               ternary defeats `branches on its truth`
  `const v = snapshot.view; if (!v)` escapes: the rule is keyed on the identifier `view`
The first of those is the one that matters: `view?: PlayView` is how a reader would most plausibly put
the nullable back, and no test in this file says a word about it. This is the list-kept-in-sync shape
CLAUDE.md names, wearing a derivation's clothes.

# [c3] **Every screen draws exactly what it drew before.** The full render sweep passes unchanged. This is…
--proof 3=met
--evidence 3=`vitest src/ui/render.test.tsx` passes 27 of 27 on 7051d8b, and the sweep is behaviour
preserving by reading: every one of the 28 sites is `?.`/`??` on an operand `openUniverse` now
guarantees (src/runtime/openUniverse.ts returns a session on every input), or a branch whose condition
is now constant. Aimed 22 mutations at the swept lines themselves rather than at the render tests —
one per file the sweep touched — and 17 died on the render test that names them (`npm run mutate`,
2026-08-17, manifest at
C:\Users\yonat\AppData\Local\Temp\mutations-the-shell-is-never-handed-a-missing-view-pass1.json;
25 killed, 5 survived, 0 unstable, 0 errored over the 30 entries). The four new render proofs in
af9ce0e each kill the line they were written for: the two banners, the Local standing and the modal's
way out. The five survivors are filed below rather than left implied; none of them is a screen drawing
something different today, and three are decisions no static render can reach.
One caveat on the record: the sweep was specified as "either an unreachable branch to remove or a `?.`
to unwrap", and src/ui/driver.ts:278 is neither — `logging(result.view)` became
`[{ kind: 'view', view: context.view, ... }]`, substituting a different expression and deleting a
conditional. It is equivalent today only because `driveChoice` in src/runtime/command.ts:1349 is the one
producer of `result.live` and always carries `view: opening`, which `settle` moves onto `ctx.view`.
`CommandResult` types `view?` and `live?` independently, so nothing enforces it, and both the deletion
of that line and its substitution for `current.view` SURVIVED the whole suite.

# [c4] `npm run tasks -- merge-ready` passes before the spec is marked done.
--proof 4=met
--evidence 4=Every mechanical leg passes on 7051d8b: tsc ok, npm test ok (3749 of 3749), layer-check ok,
audit-status ok, doctor ok with 27 warnings that do not fail the leg, bytes ok, tree ok, base ok, and
`spec the-shell-is-never-handed-a-missing-view` ok — every declared member closed. Graded on the same
reading the worker's 2026-08-17 decision on the-shell-draws-what-the-session-answers-clause-14 used and
recorded in docs/events.jsonl: the remaining red legs are the audit-pass accounting, which no run of the
gate can turn green. Two things a reader of this record should know rather than re-derive.
First, the accounting is not only self-referential: `clauses opening-a-universe-answers-rather-than-raises`
clears when the sibling auditor files, and this spec's leg clears when this pass is filed, but
`spec the-shell-draws-what-the-session-answers` (1 open member) and `clauses` (c4, c5, c14 outstanding
across 2 passes) need a third audit on a spec nobody has been commissioned for. The branch cannot go
green until that pass exists.
Second, `npm test` FAILED on the first of three merge-ready runs and passed on the other two, and a bare
`npm test` between them passed 3749 of 3749. That is the fourth sighting of
npm-test-flakes-on-three-slow-spawn-heavy-tests-under-full-s and not this branch's doing; a recurrence
is written into the friction file beside this one.

# One block per finding, uncommented. A finding needs both halves: what is broken
# (--evidence) and what fixing it would mean (--deliverable), and it names what is
# at fault: the tooling, the contract that briefed the work, or nobody — nobody being
# a real answer, for a question no one could have answered when the work was briefed.

--finding The driver publishes two more fields whose absence the door made unreachable, and a component draws a branch for one of them
--severity high
--system User interface
--fault contract
--file src/ui/driver.ts
--deliverable One derivation over the published type instead of three sweeps of one field each: a
type-level assertion in src/ui that walks `DriverSnapshot`'s own keys and names the fields that may
honestly be absent, so `speed: number | null`, `serialized(): string | null` and a future
`view?: PlayView` all fail the same tsc that already runs, and nothing has to be kept in sync with it.
Then `speed: number`, `serialized(): string`, the dead guard out of SettingsPane.tsx:62, the stale
"null when there is no session to save" comment off driver.ts:63, and the `!` off
scripts/drift.test.ts:236. That derivation also retires c2's hand-written union rule, which is why it is
one change and not four.
--evidence 2026-08-17, on 7051d8b. src/ui/driver.ts:43 `speed: number | null` while :151 sets it from
`context.live.speed`, typed `number` at src/runtime/command.ts:131, with `context` definitely assigned
before the constructor returns. src/ui/driver.ts:66 `serialized(): string | null` while the body is
`serializeSession(context.session)`, typed `string` at src/runtime/session.ts:395. Both were genuinely
nullable at the merge base 7a0081a (`context ? ... : null`) and were made total by this branch's own
commit cedfaf5, which left the types behind. The consequence is already in the tree:
src/ui/SettingsPane.tsx:62 `{speed === null ? null : <SpeedField .../>}`. `npm run mutate` over the whole
suite: removing that guard SURVIVED 0 failed of 3749, and inverting it — which removes the speed control
from the Settings page — also SURVIVED 0 failed of 3749. A branch that is dead and a branch that is
untested look identical there, which is the Deliverable's own sentence about what a lying type costs.

--finding c2's rule derives its files and enumerates its spellings, and the likeliest way of putting the nullable back is not on the list
--severity high
--system User interface
--fault contract
--file src/ui/surface.test.ts
--deliverable Ask the type rather than the text for the half that can be asked of the type: a
derivation over `DriverSnapshot`'s keys that fails tsc for any field admitting null or undefined
answers `PlayView | null`, `PlayView | undefined` and `view?: PlayView` as one question instead of
three spellings, and answers it for every field rather than for the one named `view`. Keep the text
scanner for the truthiness spellings it does cover, and either widen it to `if (<it>)`, loose `==`/`!=`
and a multi-line ternary, or say in the file that those are out of its reach — a rule whose gaps are
written down does not pretend to be a derivation.
--evidence 2026-08-17, on 7051d8b. The rule is eight regexes over the text of every module under
src/ui, and the test that grades it checks those eight against eight hand-written spellings, which is
enumeration on both axes. Re-run by pasting `A_VIEW`, `NOTHING`, `ASKING` and `UNION` out of
src/ui/surface.test.ts into `node -e`: `view?: PlayView` escapes the union rule entirely, `if (view)`
escapes all eight, `view == null` and `view != null` escape because the pattern needs `[=!]==`,
`typeof view === 'undefined'` escapes on the quote, `Boolean(view)` escapes, a ternary wrapped onto a
second line escapes because the scan is line-by-line, and renaming the binding escapes because the rule
is keyed on the identifier `view`. `view?: PlayView` is the one that matters: it is the standard
TypeScript spelling of the union the rule bans, and src/runtime/command.ts:102 already writes
`view?: PlayView` one layer down, so it is the house spelling rather than an exotic one.

--finding The GUI driver's arming log is unobserved in both directions, so the one line this sweep rewrote rather than unwrapped could not be verified
--severity medium
--system User interface
--fault contract
--file src/ui/driver.ts
--deliverable Extends the-arming-log-in-the-gui-driver-is-redundant-with-the-tick-, which measured that
deleting the line passes. The stronger measurement is that substituting a different view passes too, so
the choice this branch made there is unverifiable rather than merely redundant. Either a fixture where
the world speaks as an action begins — which is the one thing the arming view carries that the tick(0)
view cannot, because `said` is drained — or the line goes; and either way `CommandResult` should tie
`live` to `view` at the type, since the substitution is only correct because `driveChoice` happens to be
the sole producer of a live result.
--evidence 2026-08-17, on 7051d8b. Commit f85c80b turned
`appendOutputs(current.transcript, logging(result.view))` into
`appendOutputs(current.transcript, [{ kind: 'view', view: context.view, reread: false }])` at
src/ui/driver.ts:278, which is a substitution and a deleted conditional rather than the `?.` unwrap the
spec's Deliverable said every site would be. `npm run mutate` over the whole suite, both directions:
deleting the line SURVIVED 0 failed of 3749, and replacing `context.view` with `current.view` SURVIVED
0 failed of 3749. `CommandResult` at src/runtime/command.ts:102 and :108 declare `view?` and `live?`
independently, so a live result carrying no view would make the new line log a stale view where the old
one logged nothing, and nothing in the tree would say so.

--finding A third decision inside a src/ui component that no static render reaches, on a line this sweep touched
--severity low
--system User interface
--fault nobody
--file src/ui/App.tsx
--deliverable Extends two-decisions-inside-src-ui-components-that-only-a-click-can from two to three:
`useArrivals(view.discovered)` at src/ui/App.tsx:182 joins MapPane's recentre floor and SkillsPane's
`now.at`. The pure half, `newlyFound`, is already covered at src/ui/discovery.test.ts:124-132; what is
unreachable is App handing it the view's own `discovered`. Same deliverable as the record it extends —
the decision belongs in a `.ts` beside the component, or the wiring is declared untested in one place
rather than discovered one mutation at a time.
--evidence 2026-08-17, on 7051d8b. `npm run mutate`: `useArrivals(view.discovered)` replaced with
`useArrivals([])` SURVIVED 0 failed of 3749, escalating through
src/ui/render.test.tsx "acknowledges the place that has just arrived, and leaves the known one alone"
and the whole of render.test.tsx first. That named test drives MapPane directly with a literal
`arrivals={['surveyed.overlook']}` and says so in its own comment, so it cannot see App's wiring at all.

--finding The dev speed control has no render proof, and c10 of the-shell-draws-what-the-session-answers is graded met
--severity medium
--system User interface
--fault nobody
--file src/ui/SettingsPane.tsx
--deliverable A render proof for the Settings page in src/ui/render.test.tsx of the shape the file
already uses for the Edit pane's command field and the plane modal's two glyph controls: mount App on a
dev session, open Settings, and assert the speed field is drawn carrying the session's own multiplier.
Without it the whole control can be deleted from the markup and every gate stays green, which is what
lets the dead `speed === null` guard beside it sit unnoticed.
--evidence 2026-08-17, on 7051d8b. `npm run mutate` over the whole suite: inverting
src/ui/SettingsPane.tsx:62 to `{speed !== null ? null : <SpeedField .../>}`, which renders nothing where
the field is, SURVIVED 0 failed of 3749. The existing proofs for the multiplier
(src/ui/devMode.test.tsx:275-298) all read `driver.snapshot().speed` and never render the pane, and
surface.test.ts's `data-drive` rule reads the source text rather than the markup, so nothing in the tree
asserts the control reaches a screen. The control was added on this branch by ec5370d and 9938641.

--finding A comment in render.test.tsx now introduces a test sixty lines away from it
--severity low
--system User interface
--fault nobody
--file src/ui/render.test.tsx
--deliverable Move the "The field itself is DOM wiring and is not driven here" comment back above
`draws the command field on Edit`, which is the test it describes. Two comment blocks now sit stacked
with the four new tests inserted between the first and its subject, so the file reads as if the banner
tests were about a command field.
--evidence 2026-08-17, on 7051d8b. src/ui/render.test.tsx:547-549 is the DOM-wiring comment and
:550-554 is the new banners comment; the test the first one describes is at :613. Introduced by af9ce0e,
which inserted the four new proofs at the comment's old position.
